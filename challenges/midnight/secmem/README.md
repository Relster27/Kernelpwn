# Infos

- the user you can connect with : user (password : user)
- If your qemu version support the cpu requested you can use yours and avoid using the container
- You can also download the required libraries for the qemu binary in the folder like the Dockerfile do, to debug on your host
- There is an ssh server to drop your exploit and run it
